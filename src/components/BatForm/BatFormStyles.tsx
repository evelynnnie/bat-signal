import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  text: { color: '#E5BF3C', marginBottom: 4 },
  input: { backgroundColor: '#fff', marginBottom: 10, borderRadius: 4, padding: 8 }
});

export default styles;